``importnow()`` When typing ``pip install`` and ``import`` takes too damn long
===========================================

``importnow(torch)``
``IMPORT(numpy)``


This error is for losers ``ImportError: No module named``

Are you a winner? Or are you a loser?